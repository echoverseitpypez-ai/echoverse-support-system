# Server env
PORT=4000
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# CORS - comma-separated allowed origins (leave empty for all origins in dev)
CORS_ORIGIN=http://localhost:3000,https://your-app.vercel.app

# Optional SMTP
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
